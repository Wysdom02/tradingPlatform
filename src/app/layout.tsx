import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Real-Time Orderbook Viewer',
  description: 'A real-time orderbook viewer with order simulation capabilities for multiple cryptocurrency exchanges.',
  keywords: 'orderbook, cryptocurrency, trading, market depth, order simulation',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
